import 'package:flutter/material.dart';

class HomeActivePage extends StatelessWidget {
  const HomeActivePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        padding: EdgeInsets.fromLTRB(20, 40, 50, 60),
      ),
    );
  }
}
